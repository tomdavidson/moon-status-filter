import {render, screen} from '@testing-library/react';

import { {{pascalCase name}} } from './{{kebabCase name}}';

describe('{{pascalCase name}}', () => {
  it('renders', () => {
    render(<{{pascalCase name}} />);

    expect(screen.getByText("{{pascalCase name}}")).toBeDefined();
  });
});
