

// #!/usr/bin/env -S ts-node-esm --compilerOptions='{"module": "es2022"}'

// import 'zx/globals';

// const foo = (num: number): number[] => [num, num * 2, num * 3];

// await $`echo it works ${JSON.stringify(foo(1))}`;
